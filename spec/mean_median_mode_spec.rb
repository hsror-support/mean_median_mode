require 'spec_helper'

describe MeanMedianMode do
  it 'has a version number' do
    expect(MeanMedianMode::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
